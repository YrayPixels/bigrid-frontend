/**
 * Bizgrid Sell PWA app version (human-readable cache family).
 * Keep in sync with `VERSION` in `public/sw.js`.
 * Bumped by the pre-push hook when pushing to main (see .githooks/pre-push).
 * Deploy detection also uses `BUILD_ID` stamped into `sw.js` at build time.
 */
export const PWA_VERSION = "1.0.6";
