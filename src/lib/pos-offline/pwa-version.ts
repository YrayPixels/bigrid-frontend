/**
 * Bizgrid Sell PWA app version.
 * Keep in sync with `VERSION` in `public/sw.js`.
 * On pushes to `main`, CI bumps the patch via `scripts/bump-pwa-version.mjs`.
 */
export const PWA_VERSION = "1.0.0";
